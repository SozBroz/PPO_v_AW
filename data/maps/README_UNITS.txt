Optional per-map file: <map_id>_units.json next to <map_id>.csv

AWBW's public text/csv map download is terrain tiles only (see text_map.php on the site). Predeployed units are not included. To mirror AWBW starting armies, maintain a sidecar JSON — see engine/predeployed.py for the schema.
